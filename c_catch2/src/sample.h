#ifndef SAMPLE_H
#define SAMPLE_H

#include <stdbool.h>

bool isLeapYear(int year);

int MAX_FIZZBUZZ_SIZE = 9;
// max size of the char* result in this function is MAX_FIZZBUZZ_SIZE
void fizzBuzz(char* result, int n);

int closestToZero(int input[], int inputLength);

#endif //SAMPLE_H
